/**
 * Placeholder until the full vault workspace lands in T16; the route and the
 * session boundary are already the real production wiring.
 */
export function VaultPlaceholder() {
  return (
    <div className="mx-auto max-w-screen-xl px-4 py-16">
      <h2 className="font-display text-3xl font-bold">保险库</h2>
      <p className="mt-3 font-body text-sm text-neutral-600">工作区正在建设中（T16）。</p>
    </div>
  );
}
