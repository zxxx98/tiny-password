/**
 * Build-time configurable, NON-SENSITIVE default server address. Only the
 * address lives here (design §4.6); nothing sensitive persists. Override per
 * build by editing this constant; the user can always change the address on
 * the sign-in screen.
 */
export const DEFAULT_SERVER_URL: string = '';
