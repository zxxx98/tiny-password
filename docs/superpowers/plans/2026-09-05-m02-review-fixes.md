# M02 Review Fixes Implementation Plan

**Goal:** 修复已审查确认的四项 M02 缺陷。
**Architecture:** 保持 HTTP 合同；成员写事务获取 SQLite 写锁并校验当前会话/账号；以主密钥作域隔离 HMAC 派生幂等密钥；审计游标绑定 event；重复禁用返回当前 User。
**Tech Stack:** Go、SQLite、现有 HTTP 集成测试。

用户已授权修复审查结果，本次在当前工作区顺序执行。

- [x] 添加并运行回归：请求体读取期间撤销会话后创建拒绝；失效主体不能执行成员写操作；重复禁用返回完整 User。
- [x] 在 users 写事务首条语句重验会话、期限、账号状态和角色；重复禁用从事务读取当前行。
- [x] 添加并运行跨事件游标拒绝测试；将 event 纳入签名上下文。
- [x] 添加并运行重启重放测试；由主密钥派生稳定且域隔离的幂等指纹密钥。旧随机密钥记录无法恢复，保留其到期冲突边界并记录。
- [x] 执行定向测试、全量 Go/race/vet 与 diff 检查，更新验收记录。
